apiVersion: v1
kind: Service
metadata:
  name: web-app-service
  namespace: tenant-a
  labels:
    app: web-app
spec:
  type: ClusterIP
  selector:
    app: web-app
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
