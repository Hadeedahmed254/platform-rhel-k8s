apiVersion: v1
kind: Service
metadata:
  name: mariadb-service
  namespace: tenant-a
  labels:
    app: mariadb
spec:
  type: ClusterIP
  clusterIP: None  # Headless service for StatefulSet
  selector:
    app: mariadb
  ports:
  - port: 3306
    targetPort: 3306
    protocol: TCP
    name: mysql
